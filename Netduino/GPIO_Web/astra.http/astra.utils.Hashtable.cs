using System;
using Microsoft.SPOT;
using astra.http;

namespace astra.utils
{
    public class Hashtable
    {
        protected StringBuilder atts = new StringBuilder();
        protected StringBuilder vals = new StringBuilder();
        
        public Hashtable()
        {
        }

        public void put(String att, String value)
        {
        }

        public Object get(String att)
        {
            return "";
        }
    }
}
